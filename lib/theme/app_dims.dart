import 'package:flutter/widgets.dart';

/// Shared shape and spacing scale for the modern minimal look.
class AppDims {
  AppDims._();

  /// Cards and illustration containers.
  static const rCard = 24.0;

  /// Inputs and nested tiles.
  static const rInner = 16.0;

  /// Bottom sheet top corners.
  static const rSheet = 28.0;

  /// Buttons, chips, progress bars.
  static const rPill = 999.0;

  // 4pt spacing scale.
  static const s4 = 4.0;
  static const s8 = 8.0;
  static const s12 = 12.0;
  static const s16 = 16.0;
  static const s20 = 20.0;
  static const s24 = 24.0;
  static const s32 = 32.0;

  static const pagePad = EdgeInsets.symmetric(horizontal: 20);
}
